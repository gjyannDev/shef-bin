<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Category</title>
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body class="bg-[var(--base-white)] text-[var(--base-black)]">
    <%- include('../partials/header') %>

    <main class="container flex flex-col mx-auto px-15 mt-6">
      <div class="flex flex-col gap-6">
        <button type="button" class="w-fit cursor-pointer text-lg">
          <a href="/"> ← Back</a>
        </button>
        <h1 class="font-extrabold text-4xl max-w-lg leading-[1.3]">
          Add New Category
        </h1>
      </div>
      <form
        method="POST"
        action="/categories/add"
        class="flex flex-col gap-8 py-14"
      >
        <div class="flex flex-col gap-2">
          <label for="categoryName" class="font-bold text-lg"
            >Category Name</label
          >
          <input
            type="text"
            placeholder="Meat & Poultry"
            name="categoryName"
            class="w-full max-w-lg h-12 rounded-sm shadow-md placeholder:text-[var(--base-black)] focus:ring-2 border border-gray-300 dark:border-gray-700 lg:max-w-sm lg:h-11 px-4"
            required
          />
        </div>
        <div class="flex flex-col gap-2">
          <label for="imageUrl" class="font-bold text-lg">Image URL</label>
          <input
            type="text"
            placeholder="Meat & Poultry"
            name="imageUrl"
            class="w-full max-w-lg h-12 rounded-sm shadow-md placeholder:text-[var(--base-black)] focus:ring-2 border border-gray-300 dark:border-gray-700 lg:max-w-sm lg:h-11 px-4"
            required
          />
        </div>
        <div class="flex flex-col gap-2">
          <label for="description" class="font-bold text-lg">Description</label>
          <textarea
            id="description"
            placeholder="Write the category description here..."
            rows="4"
            name="description"
            class="w-full max-w-lg rounded-sm shadow-md placeholder:text-[var(--base-black)] focus:ring-2 border border-gray-300 dark:border-gray-700 lg:max-w-sm px-4 py-3 resize-none"
            required
          ></textarea>
        </div>

        <div class="flex gap-8">
          <a
            href="/"
            class="w-fit font-semibold bg-[var(--base-white)] px-5 py-3 rounded-sm cursor-pointer inline-block text-center border border-gray-300"
          >
            Cancel
          </a>

          <button
            type="submit"
            class="w-fit font-semibold bg-[var(--primary-clr)] px-8 py-2 rounded-sm cursor-pointer text-white"
          >
            Add Category
          </button>
        </div>
      </form>
    </main>
  </body>
</html>
