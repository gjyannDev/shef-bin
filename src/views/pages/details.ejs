<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Item Details</title>
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body class="bg-[var(--base-white)] text-[var(--base-black)]">
    <%- include('../partials/header') %>

    <main class="container flex flex-col mx-auto px-15 mt-6">
      <a href="/inventory" class="w-fit text-lg">← Back</a>
      <div class="flex flex-col gap-10">
        <div class="flex flex-col gap-8">
          <div class="mt-6">
            <h1 class="font-bold text-3xl"><%=item.itemName%></h1>
            <h1 class="text-lg">
              <%=item.category.slice(0,
              1).toUpperCase()%><%=item.category.slice(1)%>
            </h1>
          </div>
          <div class="flex flex-col gap-4">
            <div class="flex gap-12">
              <div class="flex gap-2">
                <p>Quantity:</p>
                <p><%= item.quantity%> <%= item.unit%></p>
              </div>
              <div class="flex gap-2">
                <p>Status:</p>
                <p><%= item.status%></p>
              </div>
            </div>
            <p>Expiration date: <%= item.formattedExpirationDate%></p>
            <p>Added on: <%= formattedCreatedAt%></p>
            <p>Last updated on: <%= formattedUpdatedAt%></p>
            <div class="flex flex-col gap-2">
              <p>Notes:</p>
              <p><%= item.notes %></p>
            </div>
          </div>
        </div>
        <div class="flex gap-2">
          <a href="/inventory/edit/<%= item._id %>">
            <button
              class="bg-[var(--primary-clr)] rounded-sm text-[var(--base-white)] px-8 py-1"
            >
              Edit
            </button>
          </a>
          <form action="/inventory/delete/<%= item._id %>" method="POST">
            <button
              type="submit"
              class="text-[var(--base-black)] px-8 py-1 rounded-sm"
            >
              Delete
            </button>
          </form>
        </div>
      </div>
    </main>
  </body>
</html>
