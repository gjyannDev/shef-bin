<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Homepage</title>
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body class="bg-[var(--base-white)] text-[var(--base-black)]">
    <%- include('partials/header') %>
    <main class="container flex flex-col gap-24 mx-auto px-15 mt-16">
      <div class="flex flex-col gap-8">
        <h1 class="text-4xl font-bold">Quick Actions</h1>
        <div class="flex gap-8">
          <div>
            <button
              class="bg-[var(--primary-clr)] w-16 h-16 rounded-full shadow-sm flex items-center justify-center"
            >
              <a href="/categories/add">➕</a>
            </button>
          </div>
          <div>
            <button
              class="bg-[var(--primary-clr)] w-16 h-16 rounded-full shadow-sm flex items-center justify-center"
            >
              <a href="/items/add">🧾</a>
            </button>
          </div>
        </div>
      </div>
      <div class="flex flex-col gap-8 mb-16">
        <h1 class="text-4xl font-bold">Categories</h1>
        <div class="grid grid-cols-3 gap-4">
          <% categories.forEach(category => { %>
          <div class="flex flex-col gap-4 border py-5 px-4 rounded-lg shadow-md">
            <div class="">
              <h2 class="text-xl font-bold mb-2">
                <%= category.categoryName.toUpperCase() %>
              </h2>
              <p class="mt-2 text-gray-700"><%= category.description %></p>
            </div>
            <button
              type="button"
              class="flex justify-start text-[var(--primary-clr)] cursor-pointer"
            >
              Explore now →
            </button>
          </div>
          <% }); %>
        </div>
      </div>
    </main>
  </body>
</html>
